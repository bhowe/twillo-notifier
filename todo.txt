TODOS

Modify APP so  date, time is entered and saved when SMS its sent
Each "send" is an atomic transaction
Each user get s text to go to a page first person wins
Once transaction marked complete. everyone else gets transaction already booked




